#include <stdio.h>
/*
 2022.3.12 成对敲括号，printf的作用 郝斌c语言p36
 */
int main () {
	
	if
	{
		
	}
		
	if (sjja(kosq))
	{
		while(asj(ajs)iah)
		{
		}
		i=5;
		j=20;
	}//成对敲括号
	
	if (shd(sdsj))
	{
		while(sdhjs)
		{
			sdjk
		}
	}
	printf("hello https://tool.lu/\n");
	printf("adkk");
	printf("%d",j);
	printf("%ld %x/n",j,k);
	//1、printf("");
	//2、printf("%d",j);
	//3、printf("%d %d/n",j,k);
	//4、printf（”I=%d,i”）
	return 0;
}

# include <stdio.h>
//#后空格
	int main(void)
{
	//	int i;
	printf("请输入");
	/*scanf("m%d",&i)；
scanf（“%d %d”，&i，&j）；

/*输入m123，m后才是合法的输入 
%d就是输入的那个东西，只能输入0-9，以%d读取，存储为二进制代码
printf（“%d”，j）主体是%d这个把二进制东西转化成十进制了的东西。就是那个值，将二进制代码存储以%d输出
非合法字符读取到会以垃圾值输出，空的没赋值也是垃圾值
 */
	return 0;
}


